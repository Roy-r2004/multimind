You plan a Google Places Text Search grid for a DISCOVERY census of
addiction-treatment provider locations — residential, inpatient, outpatient, detox,
association/NGO-run, and generically-named facilities, regardless of ownership. This is
a discovery pass only: later stages classify ownership, funding, and client eligibility
from what is found here, so cast a wide net rather than pre-filtering by ownership or
care setting at discovery time. The grid must give real geographic coverage and use both
English and local-language search terms, because facility names and Places categories
are often only findable in the local language.

{% if state_name %}
CRITICAL STATE SCOPE:
- Target: {{ state_name }}{% if state_code %} ({{ state_code }}){% endif %}, {{ country_name }}.
- You must generate cells EXCLUSIVELY within the state/province of {{ state_name }}.
- Do NOT include any cities, counties, or regions from other states or outside {{ state_name }}.
- Cover the major cities, metropolitan areas, and counties across {{ state_name }}.
- In each query_text, explicitly include the city and the state name or abbreviation (e.g., "... Dallas, TX" or "... Dallas, Texas") so Google Places geographically anchors the search to {{ state_name }}.
{% endif %}

A country discovery profile (JSON below, may be an empty object if the discovery stage
failed for this run) may already list this country's known administrative regions,
spoken languages, and local search terminology grouped into query families such as
generic, residential, outpatient, detox, association, and acronym.

When the profile is non-empty:
- Prefer its administrative_regions for geographic coverage and its languages for
  local-language query variants.
- Draw provider_terms from every listed query family (generic, residential, outpatient,
  detox, association/NGO, acronym, and any others provided) — not only one family.
- Include common_acronyms as their own query variants where present.

When the profile is empty or missing:
- Fall back to broad generic English search terms plus your own best knowledge of the
  primary local language(s) for {{ country_name }}. Still cover outpatient,
  association/NGO, residential, detox, and generic phrasings — never restrict discovery
  to private/inpatient-only terms.

Country discovery profile (JSON):
{{ country_profile_json }}

{% if focus_region_names %}
This is an EXPANSION pass, not a fresh full-country plan. These regions already
found new facilities and should get MORE query variants — do not replan the
whole country or repeat cities/regions outside this list:
{{ focus_region_names | join(", ") }}
{% endif %}

Return strict JSON only with this shape:
{
  "cells": [
    {
      "region_name": "Minsk Region",
      "city_name": "Minsk",
      "query_text": "addiction rehabilitation center Minsk Belarus",
      "query_family": "residential",
      "query_language": "en"
    }
  ]
}

Rules:
{% if state_name %}
- Cover {{ state_name }}'s major cities, metropolitan areas, and counties — enough for thorough statewide coverage without wasteful overlap.
- Do not plan any cells outside {{ state_name }}.
- In each query_text, include the city name and the state name or abbreviation (e.g. "Dallas, TX" or "Dallas, Texas").
{% else %}
- Cover the country's major cities/regions — capital plus other large or regionally important cities, enough for real national coverage without wasteful overlap.
{% endif %}
- For each city, generate 2-4 query variants: at least one in English and at least one in the country's primary local language(s).
- Cover outpatient, association/NGO, detox, residential, and generic addiction-treatment phrasings for both substance and behavioral addictions — do not restrict discovery to private/inpatient-only terms, and do not ban outpatient, association, or NGO terms.
- Include substance addiction terms where locally relevant (alcohol, drug, opioid, cocaine, meth, heroin, cannabis dependency, etc.) and behavioral addiction terms where locally relevant (gambling, gaming, internet, sex/porn addiction treatment, etc.).
- Do not invent a target facility count.
- Do not hardcode terminology for any specific country — use only the profile above plus these generic instructions, identically for every country.
- Each query_text must include the city (or region) name so Google Places can geographically scope the text search.
- When a query clearly matches one of the profile's query families, set query_family to that family's name (e.g. "outpatient"); otherwise omit it or set it to null.
- Set query_language to the language used in query_text (e.g. "en" or the local language name/code) when known; otherwise omit it or set it to null.
- Do not exceed {{ max_cells }} total cells.
{% if state_name %}
- Do not invent regions/cities that do not exist in {{ state_name }}, {{ country_name }}.
{% else %}
- Do not invent regions/cities that do not exist in {{ country_name }}.
{% endif %}
- region_name and city_name should be in a commonly recognized English or transliterated form; query_text may be in the local language.
- Do not include explanations outside the JSON object.

Country: {{ country_name }} ({{ country_code }})
{% if state_name %}Target State: {{ state_name }}{% if state_code %} ({{ state_code }}){% endif %}{% endif %}
