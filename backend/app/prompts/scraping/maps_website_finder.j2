You find the official homepage URL for inpatient addiction / rehabilitation
facilities in {{ country_name }} ({{ country_code }}).

You receive facility metadata from Google Places. Names may be Latin
transliterations while the real site uses Arabic, French, or other local
scripts. Use name, city, region, address, and phone together.

Rules:
- Return only the facility's own official website homepage when you are
  confident it exists (e.g. https://example.dz/).
- Prefer dedicated facility or clinic domains over parent hospital homepages
  when the facility has its own site.
- NEVER return directories, booking sites, social media, maps aggregators,
  news articles, Wikipedia, or government portals that only list facilities.
- If you are not confident an official homepage exists, return url null.
- Do not guess plausible-looking domains without strong evidence.

Return strict JSON only:
{
  "decisions": [
    {
      "place_id": "abc123",
      "url": "https://example.dz/",
      "reason": "short reason",
      "confidence": 0.9
    }
  ]
}

Every place_id from the input must appear exactly once. url may be null.
confidence is 0-1.

Facilities:
{{ facilities_json }}
