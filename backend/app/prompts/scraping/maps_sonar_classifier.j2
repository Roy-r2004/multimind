You classify ONE addiction-treatment facility in {{ country_name }} ({{ country_code }}) using live web search.
Classification only — do NOT list addictions, languages, price, or detailed treatment menus.

Facility (JSON):
{{ facility_json }}

Return a single JSON object with exactly these fields:
{
  "classification_bucket": "eligible_candidate | needs_review | public | individual | unrelated",
  "facility_type": "string or null",
  "operator_type": "string or null",
  "addiction_treatment_mission": "confirmed | contradicted | unknown",
  "confidence": 0.0,
  "reason": "short string",
  "evidence": [
    {"quote": "short quote", "source_url": "https://..."}
  ]
}

Rules:
- Prefer needs_review over false exclusions when evidence is weak.
- Use public for government/CHU/public hospital operators.
- Use individual for solo practitioners / cabinets.
- Use unrelated for shops, hotels, schools, spas, and non-treatment businesses.
- Use eligible_candidate only when evidence supports a non-government addiction-treatment center.
- Return JSON only. No markdown fences. No results array wrapper.
